

const state = {
    input: ""
};

const getters = {
    savedData: (state) => state.savedData
};

const actions = {
    
};

const mutations = {};

export default {
    state,
    getters,
    actions,
    mutations
};